'use strict';

module.exports = function fetchApprovalRecord(seneca, opts) {

    var shared = this,
        approvalTable = opts.tables.approval,
        logLevel = opts.logLevel

    return handler;

    // -----------------------------------------------------------------------

    function handler(console, state, done) {
        console.info("Fetch Approval Record");
        done(null, state);
    }
}
